# ============================================================================
#  WF · 功函数绘图工作台 —— 运行依赖
#
#  安装：pip install -r requirements.txt
#  说明：程序为 PySide6 (Qt6) 桌面 GUI，使用 matplotlib 绘图、numpy 处理数据。
# ============================================================================

PySide6>=6.5,<7          # Qt6 界面（窗口、控件、样式）
matplotlib>=3.7          # 曲线绘制
numpy>=1.24              # 数值计算
