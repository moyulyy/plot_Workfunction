# ============================================================================
#  WF · 功函数绘图工作台 —— 打包依赖（可选）
#
#  安装：pip install -r requirements-build.txt
#  打包：pyinstaller --noconfirm --clean WF-Viewer.spec
#        （或直接双击 build_portable.bat）
#
#  仅用于把程序打包成 Windows 便携版 exe；运行程序本身不需要 PyInstaller。
# ============================================================================

-r requirements.txt
pyinstaller>=6.5         # 打包为独立 exe
